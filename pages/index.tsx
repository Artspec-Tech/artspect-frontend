import type { NextPage } from "next";
import Layout from "../components/layout";

const Home: NextPage = () => {
  return (
    <Layout>
      <div>Hello, world!!!!</div>
    </Layout>
  );
};

export default Home;
